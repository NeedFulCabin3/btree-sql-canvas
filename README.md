# btree-sql-canvas
A browser-based B-Tree relational storage engine and handcrafted SQL query processor paired with a real-time pixel processing pipeline for direct client-side snapshot indexing and structured queries without backend dependencies or external server infrastructure.
